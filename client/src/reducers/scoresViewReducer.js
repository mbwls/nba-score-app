import { actionTypes } from '../actions/scoresViewAction';

const initialState = {};

export const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
